<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.edupedia">

    <dist:module dist:instant="true" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.FrontPageUI">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:name=".ui.RegisterUI"
            android:label="@string/title_activity_register_ui"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.StartUI"
            android:label="StartUI"/>
        <activity
            android:name=".ui.MainNavigationUI"
            android:label="MainNavigationUI" />
    </application>

</manifest>